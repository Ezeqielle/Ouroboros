const test = (character) => {
    const {name, role} = character
    console.log(name)
    console.log(role)
}


character = {
    name:"makilock",
    role:"dps",
    hp:100,
    level:120
}

test(character)